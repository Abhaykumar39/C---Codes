#include<stdio.h>
#define max_size 10
int push(int stack[],int top)
{
    int[element]
    if(top>=max_size-1)
    {
        printf("%d",stack is full);
        else{
            printf("enter the value");
            scanf("%d",&x);
            top++;
            stack[top]=elememt;
        }
        return 0;
    }
}
    
